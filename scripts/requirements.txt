# cifsQuant Python Dependencies
# ============================================================================
# Pip fallback for non-conda users.
# RECOMMENDED: use environment.yaml with conda instead (see setup_environment.sh).
#
# Install:
#   pip install -r scripts/requirements.txt
# ============================================================================

# ── Core scientific stack ──────────────────────────────────────────────────
numpy>=1.24,<2.0       # pin <2.0 for numba/torch compat
pandas>=2.0
scipy>=1.12
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.3
scikit-image>=0.21
statsmodels>=0.14
joblib>=1.3
tifffile>=2023.1
pyyaml>=6.0
networkx>=3.3

# ── Single-cell analysis ───────────────────────────────────────────────────
anndata>=0.10
scanpy>=1.9
umap-learn>=0.5
numba>=0.58            # required by umap-learn

# ── Spatial / graph clustering ─────────────────────────────────────────────
shapely>=2.0
igraph>=0.11
leidenalg>=0.10

# ── SCIMAP ─────────────────────────────────────────────────────────────────
scimap>=2.3

# ── SpatialCells ───────────────────────────────────────────────────────────
# NOT on PyPI. Install from source after cloning:
#   pip install -e /path/to/SpatialCells
# See setup_environment.sh for auto-detection logic.

# ── Optional: GPU acceleration ─────────────────────────────────────────────
# Uncomment to match your CUDA version (speeds up illumination correction):
# cupy-cuda11x   # CUDA 11.x
# cupy-cuda12x   # CUDA 12.x
